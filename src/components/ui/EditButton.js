import './css/EditButton.css';

const EditButton = (data) => {

    return (
        <div>
            <button>
                Edit Amount
            </button>
        </div>
    );
}

export default EditButton;